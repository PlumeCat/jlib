{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/Debug/jlib",
            "args": [],
            "preLaunchTask": "Build - Debug",
            "cwd": "${workspaceRoot}",
        }
    ]
}
