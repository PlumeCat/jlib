{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/build/jlib",
            "args": [],
            "preLaunchTask": "Build - Debug",
            "cwd": "${workspaceRoot}",
        }
    ]
}
